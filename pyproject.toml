[project]
name = "mail-haven"
version = "0.1.0"
description = "Simple user management for email accounts (CLI & web)."
authors = [{name = "Axentx"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
